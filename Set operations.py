#SET OPERATIONS:

S = {'apples', 'mangoes'}
T = {'bananas', 'oranges', 'apples' }
print(S.union(T)) #Combines two sets
print(S.intersection(T)) #Prints common elements
print(S.difference(T)) #Removes the elements that are common in both sets from the first
