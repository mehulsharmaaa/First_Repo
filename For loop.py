#FOR LOOP:

L = ['appple', 'mangoes', 'bananas']
for items in L: #Used for iterating over a sequence
    print(items)

M = 'Example'
for word in M: #For loop in string
    print(word)

print(items) #If we know print iterator from the first example it'll print the last element from the sequence as it was stored as a value lastly
